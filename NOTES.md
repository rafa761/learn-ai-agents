# NOTES.md — teaching working notes

## Learner profile (2026-09-07)
- Rafael, Senior Python developer (deep: types-adjacent Python — pydantic/asyncio idioms land well).
- Earned **Claude Architect Foundation certification** ~2026-09-05. Knows Claude ecosystem concepts, MCP at a conceptual level, prompting. Do NOT re-teach agent vocabulary from scratch — treat as known floor.
- TypeScript: no claimed experience. JavaScript itself is also new — do not assume JS fluency when choosing TS docs (prefer "TS for the New Programmer" entry, not "TS for JavaScript Programmers").
- Native pt-BR; works in English. Lessons written in English (his request language).

## Preferences & style
- Wants to *build* (learn-by-construction). Video roadmap + book as anchors; lessons should always end in working code he ran himself.
- **Toolchain (2026-09-07, explicit): pnpm only — never npm/npx in lesson instructions. ONE root `package.json` + ONE root `tsconfig.json` for the whole repo; labs are bare folders of `.ts` files (no per-lab scaffolding, ever); all commands run from the repo root.**
- **Math exposure (2026-09-08, explicit): RAG's math is his first contact — "no previous math foundations." Explain whys AND hows, plainly (geometric intuition over algebraic formalism), without exaggeration or padding. Wants true foundations, time-efficient. Do not skip the math; do not dress it up either.**
- Persona for the assistant: JARVIS (see global CLAUDE.md) — irrelevant to lesson content, but keep chat tone consistent.

## Environment facts
- Book PDF: `~/Documents/Livros/AI engineering/book-The Hitchhiker's Guide to Agentic AI - 2026.pdf` (note the Unicode apostrophe ’ in filename — always quote). 636 pp, v1.3, arXiv:2606.24937v2. Text extractable via `pdftotext` (regenerate to /tmp/opencode/hhgtg-full.txt when needed — /tmp evaporates on reboot).
- Also in ~/Downloads: "The AI Engineering Bible" EPUB (unrelated download, not the roadmap book; possible secondary resource later).
- Repo: was branch `learning/ai-agents-ts`; Rafael merged it to `main` via PRs #1–#2 and deleted the branch (observed 2026-09-07, session 5). Course now lives on main by his hand.
- Roadmap video identified: "How I'd Become an AI Engineer in 2026 (Even with No CS Degree)" (youtube 8c8Rrhd2oxo, playlist PLUs4VCkQzu7t9kdyCaSN1r6M2IwDfDScj). Channel not yet identified.
- Node v26.8.1; pnpm 12.3.4. Root `package.json`: type module, `typecheck` script (tsc --noEmit), devDeps @types/node/tsx/typescript@7. Root `tsconfig.json`: nodenext, es2022, types ["node"], strict, noEmit. `pnpm-workspace.yaml` exists (allowBuilds: esbuild). Repo-root `.env` (gitignored) holds ANTHROPIC_API_KEY + ZAI_API_KEY; labs load it via `pnpm exec tsx --env-file=.env labs/…`.

## Course architecture decisions
- Book chapters mapped to mission: skip Parts II–III (RL training). Core: ch. 15 (agentic intro), 16 (RAG), 18 (agent harness), 19 (loop engineering), 20 (design patterns), 22 (MCP), 23 (agent skills), 25 (multi-agent), 26 (frameworks), 27 (agentic UI, incl. Vercel AI SDK §27.4).
- Book's code listings are Python/LangChain → every lesson translates to TS. The phrasebook (reference/) is the standing bridge.
- Lesson cadence: one tight lesson per session, always with (a) a runnable win that works offline, (b) a quiz (storage-strength focus), (c) a primary-source citation, (d) stretch with real API when key available.
- MCP currency warning: SDK v2 renamed packages to @modelcontextprotocol/server + @modelcontextprotocol/client (2026-07-28 spec). Pre-v2 tutorials show dead imports — warn whenever MCP comes up.

## Decisions (resolved in session-1 follow-up)
- **Capstone: CONFIRMED** — RAG document-Q&A agent over the Hitchhiker's Guide itself.
- **API keys available:** `ANTHROPIC_API_KEY` **and** a GLM/Z.AI key. Lesson 0002 runs live round-trips; GLM's Anthropic-compatible endpoint is a candidate second model for cost/comparison exercises.
- `.omo/` ignored in git — runtime state, not course material.

## Session log
- **2026-09-11 (session 12):** Lesson 0006 completed by Rafael — quiz 4/5 first-attempt. Sole miss = Q2 (faithfulness-ratio reading: 0.75 = one CLAIM in four lacks context support; every distractor re-attaches the ratio to retrieval artifacts — chunks/citations/documents) → LR-0006; Q2 to be interleaved verbatim into 0007's quiz. Both of 0006's interleaves retrieved correctly: Q4 (LR-0005, weights-default) healed at its first return, one lesson after the miss → interval expands, next ≈ 0009; Q5 (LR-0003, model-emits-JSON) correct at its first expanded-interval return, second consecutive → interval expands again, next ≈ 0011 (retire on correct). Interleave scoreboard 6/6 across four LRs (+ 0003 cold retake 5/5) — the spacing loop has never missed. Fresh Q1 (levels-compounding) and Q3 (MRR-vs-hit-rate) both correct; third consecutive lesson at exactly 4/5 with one fresh-question miss (difficulty calibration holding). Standing schedule: Q2 (LR-0006) → 0007, Q5 (LR-0004) → ≈0008, Q1 (LR-0005) → ≈0009, Q4 (LR-0002) → ≈0010 (retire), Q3 (LR-0003) → ≈0011 (retire). **0007 stays commissioned: the capstone** — RAG over the book itself; author with the judge's claims array surfaced before the score prints (LR-0006's second handle). Awaiting his go.
- **2026-09-08 (session 11):** Lesson 0006 authored — "Evaluate the loop" (book **§16.8** — the session-9 commissioning note said §16.6, an error: 16.6 is REFRAG decoding; corrected here and in 0005's forward pointer). Two labs, first multi-file lab of the course: `retrieval.ts` (0004 core promoted to a MODULE: copy-not-retype from rag-qa.ts — "three typings is fluency, four is typing"; `export` + `rank()` returns the FULL ranking because eval measures every k; zod seam deliberately retired at the in-process module boundary (compile-time types carry the contract) and re-erected at the judge boundary; nodenext `.js`-extension import taught, TS Handbook modules reference verified 200, added to RESOURCES) + `evaluate.ts` (golden set: 4 on-corpus rows + vogon negative probe; recall@k/precision@k/MRR hand-rolled eq. 16.22–16.24; live half: grounded answer at k=2, citation-integrity regex check, faithfulness judge eq. 16.26 — strict-JSON judge prompt, zod safeParse at boundary #3, **judge enumerates / code divides**). Golden-set annotation determined EMPIRICALLY via a temp probe (removed before publish): the "three limitations" question straddles chunks 0+1 → multi-gold row, recall@1 = 0.875 with BOTH golds scoring under 0.4 (the threshold-is-a-bug doctrine with live proof); k-sweep makes k=2 the metrics' verdict (recall@2 = 1.000; precision@3 = 0.417 = over-retrieval dilution) — the judged answer ships k=2, "defaults are hypotheses; metrics are the verdict" is the lesson's spine. Verified end-to-end before removal: typecheck clean; full live run 2026-09-08 (metrics byte-identical to 0004/0005 scores — determinism across three sessions; answer cited correctly; judge 5 claims, 5 supported, faithfulness = 1.00); no-key path verified by temporarily moving .env (metrics complete offline, judge skipped gracefully, .env restored intact). Quiz 0006: 3 fresh (levels-compounding / faithfulness-ratio-reading / MRR-vs-hit-rate, option word-counts even per question: 8/8/8/8, 7/7/7/7, 6/6/6/6) + TWO interleaves, both verified byte-equal programmatically: Q1-from-0005 (LR-0005, first return, 0-lesson gap — heals the weights-default miss where the book's failure mode #5 lives) + Q3-from-0002 (LR-0003, due ≈0006, 2-lesson gap — first expanded-interval return; explain's only change = "hand-wrote today" → "hand-wrote in lesson 0002"). New reference `reference/eval-harness.html` (three-levels table, golden-set anatomy, metrics translated w/ our verified numbers, judge contract incl. faithful-≠-relevant, failure-mode ledger 16.8.4 × which-metric-catches-it, RAGAs graduation + v0.2 API currency note). RESOURCES.md: +RAGAs docs + faithfulness metric page + TS modules reference (all verified 200 live). 0005 next-nav → live link; 0005 §7 fixed (§16.6→§16.8; "one old friend" → two). Lesson + reference render-verified in headless Chromium: 5 questions × 4 options, JSON payload mounted, full quiz click-through (score + review list + retake button render), zero console messages; screenshot at /tmp/opencode/lesson0006-top.png (visual self-check unavailable this session — model without image input; DOM+console checks passed). Standing schedule: Q1 (LR-0005) + Q3 (LR-0003) → 0006 quiz, awaiting his take; Q5 (LR-0004) → ≈0008; Q4 (LR-0002) → ≈0010, retire on correct. **0007 commissioned: the capstone** — RAG over the book itself (loader for 636 pp, vector-store graduation decision per rag-answer-loop §5, golden set grown from real questions, harness re-run on every change).
- **2026-09-08 (session 10):** Lesson 0005 completed by Rafael — quiz 4/5 first-attempt. Sole miss = Q1 (grounding why: the ONLY-context instruction exists because the model otherwise answers from its own weights) → LR-0005; Q1 to be interleaved verbatim into lesson 0006's quiz alongside the already-scheduled Q3 return (LR-0003) — 0006's quiz carries TWO interleaves. Both of 0005's scheduled interleaves retrieved correctly: Q4 (LR-0002) correct at its first expanded-interval return, second consecutive correct → interval expands again, next ≈ lesson 0010 (retire on correct); Q5 (LR-0004) correct at 1-lesson gap → next ≈ lesson 0008. Fresh questions Q2 (citations) and Q3 (top-k, his math-preference question) both correct. Standing schedule: Q1 + Q3 → 0006, Q5 → 0008, Q4 → 0010. 0006 stays commissioned (RAG evaluation — author with the faithfulness axis leaned into grounded-vs-weights, per LR-0005; the miss gives the concept a measurable second handle).
- **2026-09-08 (session 8):** Lesson 0004 completed by Rafael — quiz 4/5 first-attempt. Sole miss = Q5 (EXCEPT: generator output limit is not a chunk criterion) → LR-0004; Q5 to be interleaved verbatim into lesson 0005's quiz alongside the already-scheduled Q4 return (LR-0002) — 0005's quiz carries TWO interleaves + fresh questions. Standing schedule: Q4 + Q5 → 0005, Q3 → 0006. Also pending: 0003 cold retake (~2026-09-10, per session-6 storage-strength plan). 0005 commissioned: grounded generation.
- **2026-09-08 (session 9):** **0003 cold retake: 5/5** (storage-strength loop validated end-to-end: miss → interleave → correct at 1 session → correct cold ~3 days; 0003's quiz decommissioned from the schedule). Lesson 0005 authored — "Grounded answers": the G in RAG (book §16.2.4 translated). One lab `rag-qa.ts`: 0004 retrieval (third typing) → zod seam (`RetrievedChunk` schema, safeParse — 0003 doctrine at a new boundary) → grounded [Doc N] prompt → SDK call (`claude-haiku-4-5`, reusing his 0003 pattern incl. `process.loadEnvFile`). Verified live before removal: typecheck clean; on-corpus query → cited answer ([Doc 1]/[Doc 3]); vogon control → honest refusal, zero fabrication; retrieval scores byte-identical to 0004's run (determinism → "reproducible RAG" teaching point). Quiz 0005: 3 fresh (grounding why / citation why / top-k-not-threshold — his math preference baked in: scores are properties of PAIRS, no universal threshold, ranking dependable) + TWO interleaves, both verified byte-equal programmatically: Q4 (LR-0002, 2-session expanded interval, first expanded-interval return) + Q5 (LR-0004, 1-session interval — heals the chunk-criteria miss at code level: input budget made concrete in TOP_K). New reference `reference/rag-answer-loop.html` (prompt-contract table, top-k argument, budget arithmetic, when to graduate to a vector store, stack table). Learner preference recorded (math-newcomer, whys+hows, no exaggeration, efficient). 0004 next-nav → live. 0006 commissioned: RAG evaluation — retrieval hit-rate harness + faithfulness (book §16.6) + Q3 interleave (LR-0003, due ≈ 0006). Standing schedule: Q3 → 0006.
- **2026-09-07 (session 7):** Lesson 0004 authored — "Chunk, embed, retrieve": RAG arc opens (book ch. 16). Lab A `chunker.ts` (offline): Listing 16.2 translated — word window + overlap + eq. 16.19 formula, proven by assertions (step-tail invariant, ragged tail, loud misconfig); cosine by hand with checkable vectors. Lab B `retrieve.ts`: local embeddings via `@huggingface/transformers` 4.2.0 (added to root deps) + Xenova/all-MiniLM-L6-v2 (384d, cached in node_modules/@huggingface/transformers/.cache, 87MB, gitignored via node_modules) — model + mean pooling + normalize; hand-rolled cosine ranking; vogon control query shows the flat line. **Key pivot: live-API embeddings impossible with available keys** — z.ai global API has NO embeddings (verified live: /models = chat only; /embeddings 1211s embedding-2/3; web result's "GitHub issue #67" citation was fabricated by search — 404s, dropped; cited first-hand evidence instead). Labs verified end-to-end before removal (his to type): typecheck clean (after adding `skipLibCheck: true` to root tsconfig — transformers d.ts fails nodenext; one-line documented in lesson §3), Lab A all assertions green + 157 words → 4 chunks matching formula, Lab B correct top-hits (overlap Q → 0.541, hallucination Q → 0.487, control ≈ 0.1) AND chunk 2 straddles paragraphs 3/4 — framed as live motivation for book §16.4.3/§16.4.4 (structure-aware, parent-child). New reference `reference/chunking-embeddings.html` (pipeline + both formulas, strategy + sizing tables from book 16.4/16.3, LangChain↔TS bridge, OpenAI-compatible wire shape incl. the z.ai row, transformers.js snippet). RESOURCES.md: transformers.js entry added (URL verified 200) + z.ai no-embeddings currency note. 0003 next-nav → live link. Quiz 0004: 5 fresh questions (no interleave due — Q4 returns ≈ 0005 per schedule, Q3 ≈ 0006), equal-length options enforced. Lesson + reference render-verified in headless Chromium: 5 questions × 4 options, zero console messages. 0005 commissioned: grounded generation — [Doc N] citations prompt (book §16.2.4), zod at retriever output, vector store when needed + Q4 interleave.
- **2026-09-07 (session 6):** Lesson 0003 completed by Rafael — quiz 5/5 first attempt (no miss → no new LR). Q3 interleave (LR-0003, model-emits-JSON) retrieved correctly at 1-session spacing → interval expanded, next return ≈ lesson 0006 (mirrors Q4 ladder). quiz.js upgraded to make "revisit in a few days" actionable: extracted `buildQuiz(mount)`; score summary now carries a **"Retake from memory"** button (clears the quiz's localStorage key + rebuilds fresh — original first-attempt record lives here in the session log by design); completed-quiz reopen now disables stale options (their clicks could never re-render the frozen score). `.quiz-retake` styled in course.css. Verified end-to-end in headless Chromium on lesson 0003: fresh → answer 5/5 (score + button + attempts stored) → retake (storage null, 20 options re-enabled, survives reload) → reopen-after-completion (instant summary, all 20 options disabled, retake available), zero console errors. Storage-strength plan: Rafael re-takes 0003 cold in ~3 days and reports; Q4 returns ≈ lesson 0005, Q3 ≈ lesson 0006. 0004 (chunking & embeddings) stays commissioned, awaiting his go. Repo infra: project CLAUDE.md created with gstack skill-routing rules (Rafael's explicit choice via the gstack consent gate; commit 75c8c99) — quiz.js/course.css/NOTES.md retake changes left uncommitted for him.
- **2026-09-07 (session 1):** Workspace bootstrapped on branch `learning/ai-agents-ts`. Created MISSION.md, RESOURCES.md (37 verified sources via librarian agent), assets (course.css, quiz.js), reference phrasebook, Lesson 0001. Quiz state keyed `learn-ai-agents:lesson0001` in localStorage.
- **2026-09-07 (session 2):** Lesson 0001 completed — quiz 4/5 first-attempt; sole miss = Q4 (unknown/narrowing). Recorded in LR-0002; Q4 to be interleaved verbatim into lesson 0002's quiz. quiz.js upgraded: final summary now enumerates misses (Q# + correct answer) and renders instantly on reopening a completed quiz. Learner preference captured: wants miss summaries, not just scores. Lesson Q4 wording corrected (JSON.parse is declared `any`; parking in `unknown` is the learner's move).
- **2026-09-07 (session 3, structural addendum):** Rafael normalized the repo and set the standing rule: pnpm only, ONE root package.json + tsconfig.json, labs are bare .ts folders, everything from repo root. Root configs corrected to make that true (package.json had been `type: commonjs` + npm-init cruft → rewritten clean: type module, `typecheck` script; tsconfig gained `target: es2022`). Lessons 0001 + 0002 rewritten to teach the structure: per-lab scaffold steps replaced by one-time root setup (0001) / bare `mkdir` (0002); all commands now `pnpm exec`/`pnpm typecheck` from root; `.env` now loaded as `--env-file=.env` (it lives at root). Re-verified under the real config from root: `pnpm typecheck` clean over labs/0001 (both files) + a staged labs/0002 copy; both run commands executed live. Staged labs/0002 removed afterward — the lab is his to type.
- **2026-09-07 (session 4):** Lesson 0002 completed by Rafael — lab run live, quiz 4/5 first-attempt. Sole miss = Q3 (model emits "JSON you dispatch", not runtime linkage) → LR-0003; Q3 to be interleaved verbatim into lesson 0003's quiz (perfect fit: 0003 is zod at the boundary = the validate half of validate-and-dispatch). Interleaved Q4 (LR-0002) retrieved correctly at 1-session spacing — spacing works; Q4's interval expanded, next return ≈ lesson 0005.
- **2026-09-07 (session 5):** Lesson 0003 authored — "Trust, but validate": zod 4.5.4 at the boundary + official SDK (@anthropic-ai/sdk 0.124.0, installed at root during verification). Two labs: `schema-first.ts` (offline — 0002's guards → schemas; fabricated blocks incl. malformed args + thinking-block drop) and `sdk-loop.ts` (live — SDK types the wire; `block.input` stays `unknown` forever, zod guards it). Both verified end-to-end before publishing: tsc clean, offline run, live 2-hop run, and **Exercise B's schema-drift experiment verified live**: the errors-as-results loop works mechanically, but the model GUESSED style values instead of reading the enum, burned its hops, then answered from weights — the hallucination shape; framed as predict-first exercise with "keep declaration and enforcement in sync" as the lesson. (Also disproven live: asking the model to send a wrong-typed arg — it refuses; drift is the deterministic forcing method.) Q3 interleave byte-equal on q/options/answer; explain's only change: "hand-wrote today" → "hand-wrote in lesson 0002". Adopted learner's own `process.loadEnvFile(".env")` improvement in lab code. New reference `reference/zod-boundary.html` (four moves, parse-vs-safeParse table, errors-as-results, pydantic↔zod phrasebook, SDK-types-vs-unknown table). RESOURCES.md: SDK entry added (repo + docs verified live). 0002 next-nav → live link. Staged labs removed after verification — his to type. 0004 commissioned: chunking & embeddings, the RAG module proper (book ch. 16). Branch note: `learning/ai-agents-ts` merged to main by Rafael (PRs #1–#2) and deleted — env fact above updated.
- **2026-09-07 (session 3):** Lesson 0002 authored — "The agent loop, live": raw-fetch agent loop (no SDK) with hand-written type guards (`isTextBlock`/`isToolUseBlock`/`parseReply`) fulfilling LR-0002's hand-narrowing requirement; Q4 interleaved verbatim (verified byte-equal). New reference doc `reference/anthropic-tool-loop.html` (wire cheat sheet, incl. the live-verified 400 message). Lab verified end-to-end before publishing: tsc clean + live run (model made 2 parallel tool calls in one hop, finished in 2 hops) + break-it 400 confirmed. Environment discoveries baked into lab steps: npm now installs **TypeScript 7** whose `tsc --init` template pins `"types": []` (must set `["node"]` or `process` is unknown) and defaults `noUncheckedIndexedAccess`/`exactOptionalPropertyTypes` ON; `npm pkg set type=module` required for top-level await; `npx tsx --env-file=../../.env` forwarding works. Anthropic docs domain migrated to platform.claude.com (docs.anthropic.com redirects). Lesson 0001 next-nav converted to live link. Book text regenerated at /tmp/opencode/hhgtg-full.txt (evaporates on reboot; regenerate via pdftotext — mind the Unicode ’ in the filename). 0003 commissioned: zod at the boundary + official SDK types (book ch. 18).
